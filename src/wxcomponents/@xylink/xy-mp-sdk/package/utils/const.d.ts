export declare const DEFAULT_CUSTOM_LOGGER_CONFIG: {
    fileMaxCount: number;
    fileMaxSize: number;
};
export declare const DEFAULT_INVITE_INFO: {
    meetingId: string;
    callUri: string;
    conferenceNumber: string;
    callIndex: number;
    rtmpMode: number;
    automute: string;
    roomid: number;
};
